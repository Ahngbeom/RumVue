# Elastic APM Configuration
# Copy this file to .env and update with your actual values

# Your Elastic APM Server URL
NUXT_PUBLIC_APM_SERVER_URL=http://localhost:8200

# Your application name (shows up in Kibana)
NUXT_PUBLIC_APM_SERVICE_NAME=rumvue-demo

# Service version (helps track deployments)
NUXT_PUBLIC_APM_SERVICE_VERSION=1.0.0

# Environment (development, staging, production)
NUXT_PUBLIC_APM_ENVIRONMENT=development

# ===================================================
# Source Map Upload Configuration (for npm run upload-sourcemaps)
# ===================================================

# APM Server URL for source map uploads (usually same as above)
APM_SERVER_URL=http://localhost:8200

# Service name for source map uploads (should match NUXT_PUBLIC_APM_SERVICE_NAME)
APM_SERVICE_NAME=rumvue-demo

# Service version for source map uploads (should match NUXT_PUBLIC_APM_SERVICE_VERSION)
# Use git commit hash for better tracking: $(git rev-parse --short HEAD)
APM_SERVICE_VERSION=1.0.0

# APM Server secret token (optional, only if your APM Server requires authentication)
# APM_SECRET_TOKEN=your-secret-token-here
